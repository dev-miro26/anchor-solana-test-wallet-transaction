[toolchain]

[features]
resolution = true
skip-lint = false

[programs.localnet]
anchor_solana_transaction = "GpmX2Botpq52CLg27wbEXBEN2TCod67Jrovhdsgto455"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "Localnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
client = "yarn run ts-node client/*.ts"